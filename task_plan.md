# 阶段五：衣物图片和识别能力执行计划

# 阶段七：主要缺口收敛计划

# 阶段八：审查缺口修复计划

## 目标

按代码审查优先级修复推荐业务约束、外部视觉调用超时、对象存储删除一致性、历史查询规模边界、输入校验和前端高危依赖，并逐项验证。

## 阶段

- [completed] 1. 增加 LLM 推荐结果的类别互异校验与反例测试
- [completed] 2. 为视觉识别客户端接入连接/读取超时
- [completed] 3. 修复衣物图片删除的数据库与对象存储一致性策略
- [completed] 4. 为推荐历史增加分页并消除列表 N+1 查询
- [completed] 5. 补齐反馈/风格档案输入长度约束与测试
- [completed] 6. 升级 PostCSS 依赖并重新构建/审计
- [completed] 7. 完整回归验证并记录未覆盖环境

## 验收条件

- LLM 返回同类别衣物时回退规则引擎。
- 视觉识别请求受配置的连接和读取超时限制。
- 删除图片失败不会留下不可见的数据库/对象存储不一致，且失败行为有测试。
- 历史列表有明确分页边界，查询不会按记录逐条访问反馈和衣物明细。
- 超长反馈与档案字段返回标准 400，而不是数据库 500。
- `npm audit --audit-level=high` 不再报告当前 PostCSS 高危版本。

## 阶段八完成结果

- LLM 同类别衣物结果会回退规则引擎，并有反例测试。
- 视觉识别消费连接/读取超时配置，并有慢响应测试。
- 图片删除使用 Flyway V2 清理任务表，数据库事务提交后由调度器重试 MinIO 清理。
- 推荐历史支持分页和批量反馈/衣物查询，避免无界响应与列表 N+1。
- 反馈、风格档案和格式错误 JSON 均返回标准 400。
- PostCSS 升级到 8.5.25，npm audit、后端 44 项测试、前端构建和 Compose 配置均通过。

## 目标

在不伪造外部数据、不默认消耗付费模型的前提下，消除当前原型中可以由仓库自身解决的主要缺口：可信用户身份、可演进数据库、真实趋势适配入口、视觉识别用户授权和关键反例测试。

## 第一性边界

- 身份必须由服务端校验后的认证上下文产生，不能再相信客户端自填 `X-User-Id`。
- 没有获得授权的趋势数据源时继续明确返回开发样本；代码应支持配置真实源，但不能抓取或伪造第三方热度。
- 视觉模型调用同时要求服务端启用和用户本次上传明确同意；默认关闭不是缺陷，缺少同意仍调用才是缺陷。
- Redis 当前没有业务消费者，应从部署中删除，不为满足技术栈清单制造缓存逻辑。
- 数据库结构变更必须可追踪、可重复，并在 PostgreSQL 与测试数据库上验证。

## 阶段

- [completed] 1. 审计现有认证、趋势、迁移、视觉授权和测试契约
- [completed] 2. 实现认证接口、服务端身份上下文和前端登录会话
- [completed] 3. 引入 Flyway 版本迁移并删除未使用的 Redis 服务
- [completed] 4. 实现可配置趋势适配器和视觉识别显式授权
- [completed] 5. 扩充认证、隔离、上传授权、趋势降级和浏览器 E2E 测试
- [completed] 6. 更新 README、环境变量和生产边界说明
- [completed] 7. 执行后端、前端、Compose、完整栈与浏览器对抗验证

## 验收条件

- 未认证请求访问个人接口返回 401；登录后用户只能访问自己的衣橱、档案和推荐。
- 前端不再固定发送 `demo-user`，登录状态可恢复且退出后凭据被清理。
- Flyway 在全新数据库和已有表的数据库上都有明确升级路径，`schema.sql` 不再承担运行期迁移。
- Compose 不再启动没有消费者的 Redis。
- 配置真实趋势 JSON 源时返回校验后的实时快照；源不可用时响应明确标识开发样本。
- 未勾选 AI 识别时绝不调用视觉服务；勾选后仍受服务端能力开关约束。
- 自动化测试至少覆盖认证失败、跨用户隔离、趋势失败降级、视觉授权和核心成功闭环。

## 对抗性用例

- 伪造旧 `X-User-Id` 但没有有效令牌，应返回 401 而不是读取目标用户数据。
- 用户 A 使用自己的令牌读取用户 B 的推荐或图片，应返回 404。
- 趋势源返回额外字段、空列表或无效 URL 时，不得标记为实时数据。
- 服务端已启用视觉模型但上传未提供用户同意时，识别客户端调用次数必须为 0。
- 已存在旧表且没有 Flyway 历史表时，升级不得删除原有数据。

## 遇到的错误

| 错误 | 尝试次数 | 处理 |
| --- | --- | --- |
| 并行读取三个技能文件触发 Windows sandbox CreateProcessWithLogonW 1056 | 1 | 改为单个 PowerShell 进程顺序读取，成功 |
| 受管 shell 中 `git` 从 PATH 消失且常见安装路径不存在 | 1 | 暂用直接文件检查继续；最终验证时在外部已安装环境重试 Git 检查 |
| 受管 shell 中 `mvn` 不在 PATH，无法执行代理新增的趋势/Flyway 测试 | 1 | 完成全部编辑后使用允许的外部 Maven 环境统一验证，不重复当前 PATH |
| 查询 Node 子进程命令行被系统拒绝访问 | 1 | 通过 dist 时间戳确认前端构建发生，不再读取受限进程详情 |
| 首次 `mvn test` 的 Spring 上下文无法实例化趋势适配器 | 1 | 定位为双构造器缺少 `@Autowired`；Flyway/纯单元测试已通过，标注生产构造器后重跑 |
| 并行恢复读取因受管终端找不到 `git` 导致整组命令中断 | 1 | 改为逐项容错读取；业务验证继续，最终差异检查单独定位 Git |
| Python 环境缺少 `bcrypt`，无法生成演示密码哈希 | 1 | 不安装额外依赖，改用项目已缓存的 Spring Security BCrypt 实现 |
| Git HTTPS 推送三次被连接重置 | 3 | `gh api` 可达；运行 `gh auth setup-git` 重配凭据后推送成功 |
| Docker Windows 服务无权限启动，后台 Desktop 50 秒未就绪 | 2 | 通过 Docker Desktop GUI 启动并确认 Engine running；保留数据卷重建应用栈 |
| 后端镜像首次重建时 Maven Central 响应体中断 | 1 | 前端构建已通过；为 Maven 阶段增加 BuildKit 持久缓存与 3 次传输重试后再构建 |
| 后端缓存重建超过 10 分钟工具上限 | 1 | 不重复 Compose 构建；复用已填充缓存并改用 BuildKit host 网络绕过 Docker NAT |
| Playwright 对浏览器 multipart 请求调用 `postData()` 返回 null | 1 | 删除依赖内部缓冲的断言；保留默认未勾选、必填字段、MANUAL_CORRECTED 响应，并由后端 mock 验证模型零调用 |
| 批量历史查询的单参数 lambda 匹配两个 `JdbcTemplate.query` 重载 | 1 | 显式转换为 `RowCallbackHandler`，保持批量装配方案不变 |
| 输入约束测试链式 `putArray` 构造出数组根节点 | 1 | 改为显式 `ObjectNode`；同时发现并补齐格式错误 JSON 的统一 ApiResponse 处理 |
| outbox 多文件补丁缺少更新块分隔 | 1 | 拆分为服务、新类、迁移配置三个补丁，未产生部分写入 |

## 阶段七完成结果

- 认证、Principal 数据隔离、CSRF、Flyway、授权趋势源、视觉逐次同意和 Redis 移除已落地。
- 真实旧 PostgreSQL 数据卷完成 baseline 0 -> V1，数据保留；演示种子连续执行两次稳定且账号可登录。
- Maven Docker 测试 37/37、Playwright E2E 4/4、Compose 配置、前后端镜像构建和健康检查通过。
- 应用内浏览器完成桌面与 390x844 视口 QA，控制台无 warning/error，页面和弹窗无横向溢出或重叠。

---

## 目标

在阶段一至四的衣橱与推荐链路之上，建立可验收的图片闭环：上传衣物图片 -> MinIO 保存 -> 衣物元数据落库 -> 可选视觉识别 -> 识别失败后手动修正。

## 范围边界

- 保留现有趋势、天气、个人风格接口。
- 新增衣橱数据和推荐记录的持久化。
- 推荐算法先使用可测试的开发期规则引擎，暂不接入真实大模型。
- 使用现有 `X-User-Id` 作为开发期用户边界，暂不实现完整登录。
- 使用现有开发期 `X-User-Id` 作为用户边界，不扩展完整登录。
- 视觉识别采用可替换适配器；未配置视觉模型时必须明确返回待人工确认，不伪造识别结果。
- 图片通过后端代理地址读取，MinIO 桶保持私有，避免把内部对象地址直接暴露给浏览器。

## 阶段

- [completed] 1. 确认现状并设计最小接口契约
- [completed] 2. 增加数据库配置、表结构和后端领域模型
- [completed] 3. 实现衣橱 CRUD 与规则推荐闭环
- [completed] 4. 增加控制器测试和对抗性测试
- [completed] 5. 运行构建验证并整理交付说明
- [completed] 6. 增加 MinIO 图片存储、识别适配器和衣物元数据状态
- [completed] 7. 接入前端上传、识别失败修正和图片展示
- [completed] 8. 运行后端/前端/Compose 验证并记录对抗性结果

## 验收条件

- 可以为同一用户创建、查询、删除衣物。
- 没有衣物时，请求推荐会明确失败，而不是返回伪造结果。
- 有上装、下装、鞋履时，请求推荐会返回组合、理由和推荐记录 ID。
- 推荐可以标记保存，并能通过查询接口看到保存状态。
- `mvn test` 通过，已有趋势接口行为不回归。
- 支持图片类型和大小校验，图片对象写入 MinIO 私有桶。
- 上传后返回图片代理地址、识别状态及类别/颜色/风格元数据。
- 识别失败时衣物仍可保存为待人工确认，`PUT` 修正后可进入正常推荐链路。
- 未配置视觉服务时不伪造识别成功；MinIO 不可用时不写入半成品衣物记录。

## 阶段五完成结果

- 新增 `POST /api/v1/me/wardrobe/upload` multipart 上传、`PUT /api/v1/me/wardrobe/{id}` 手动修正和图片代理读取接口。
- MinIO 使用私有桶，数据库保存 `image_url`、对象键、识别状态和识别提示；推荐过滤 `NEEDS_MANUAL_REVIEW` 记录。
- 视觉识别适配器已接入百炼多模态协议，`BAILIAN_VISION_ENABLED=false` 时明确进入人工确认，不消耗外部服务。
- 后端 24 项测试通过，前端构建通过，Compose 配置通过；真实 Docker + PostgreSQL + MinIO 上传、图片读取、跨用户 404、修正和推荐历史读取通过。
- 浏览器页面加载、上传入口、待人工确认提示和“完善 -> 保存修正”状态通过，控制台无错误。

## 阶段五剩余边界

- 当前默认关闭视觉模型；启用自动类别/颜色/风格提取时设置 `BAILIAN_VISION_ENABLED=true` 并提供可用的 `DASHSCOPE_API_KEY`，模型默认为 `qwen-vl-plus`。

## 完成结果

- 4 个后端测试全部通过。
- 后端打包通过。
- Docker Compose 配置校验通过。
- 前端构建通过，未修改前端代码。

---

# 阶段六：答辩交付增强计划

## 目标

按用户指定顺序补齐答辩演示数据、开发期边界说明、真实百炼调用证明和前端 E2E 验证。每个阶段独立验证、提交并推送到远端，避免不同任务混入同一提交。

## 执行顺序

- [completed] 1. 准备 6-8 件衣物、2-3 条推荐历史、收藏反馈及识别失败后人工修正的可重复演示数据
- [completed] 2. 集中说明 X-User-Id、规则 fallback、趋势 demoMode 和视觉识别默认关闭的开发期边界
- [completed] 3. 增加真实百炼推荐/视觉识别调用记录、流程图和 prompt 约束说明
- [completed] 4. 增加并验证前端 E2E 脚本：打开页面、添加衣物、生成推荐、保存推荐

## 提交纪律

- 开始下一阶段前，上一阶段必须完成验收并已推送。
- 每个提交只包含当前阶段直接需要的文件及必要的规划记录。
- 真实 API Key 不写入仓库；调用记录必须脱敏，但保留时间、模型、请求约束、结果和可验证标识。

## 对抗性验收

- 演示数据脚本重复运行不得无限追加重复数据。
- 无百炼 Key 时仍能明确回退，不把规则结果伪装成模型结果。
- 证明材料不得包含密钥、完整认证头或可复用隐私数据。
- E2E 必须在空数据或可控数据状态下稳定运行，并验证保存后的状态，而不只验证按钮可点击。

## 阶段六完成结果

- 演示数据脚本连续执行两次后均保持 8 件衣物、3 条推荐、1 条收藏反馈和 1 条人工修正记录。
- 四项开发期边界已有独立说明文档和 README 入口。
- 已保存真实 qwen-plus 调用 ID、usage、脱敏结果、流程图和 prompt 约束说明。
- Playwright E2E 连续两次通过，覆盖页面新增衣物、生成推荐、收藏及历史接口 saved=true 复核。
- E2E 发现并修复 Docker 前端 8090 Origin 未被后端 CORS 允许的问题。
- 前端构建、后端 24 项测试、Compose 校验和密钥泄漏检查全部通过。

---

# 阶段九：答辩交付缺口收敛计划

## 目标

在保持当前功能闭环不变的前提下，收敛答辩交付中仅由仓库自身可以消除的口径与文档缺口，避免在答辩材料中声称未实现的能力。本轮以最小改动推进，不触碰业务 Java 代码、不读取 .env、不覆盖用户未提交改动。

## 阶段

- [completed] 1. 配置口径：收敛 Actuator 暴露、移除未使用的 Prometheus 依赖、移除未使用的 pgvector 口径、固定前端 vite 依赖版本
- [in_progress] 2. 推荐审计元数据：为推荐/识别调用补齐可核对、可脱敏的审计元数据
- [completed] 3. 离线天气：评估无网络环境下天气降级与演示路径
- [pending] 4. 研究评估材料：产出对齐论文/开题等研究要求的评估材料
- [pending] 5. 毕业设计文档同步：同步 README 与开发边界文档与当前实现一致
- [pending] 6. 完整验证：执行前后端构建、审计、Compose 校验与差异检查

## 验收条件

- 安全策略保持默认不对外开放，Actuator 仅暴露 health/info。
- 仓库不再声称使用 pgvector 或提供可访问的 Prometheus 指标地址。
- 前端构建依赖版本固定为 lockfile 当前解析结果，不再依赖 latest。
- 文档口径与当前实现一致，不出现未实现能力的声明。

## 阶段九配置口径完成结果

- 安全策略保持默认不对外开放，Actuator 仅暴露 health/info，仓库不再声称 pgvector 或 Prometheus 指标地址。
- 前端 `package.json` 与 `package-lock.json` 根 `dependencies` 均固定 `vite@8.1.4`、`@vitejs/plugin-vue@6.0.7`，不再依赖 `latest`，且不改变已解析包。

## 阶段九推荐审计元数据计划

用一次可核验、可脱敏的审计元数据收敛推荐来源的“可核对”口径。只有真实成功的百炼响应才写入 providerCallId 与三类 token；规则降级不伪装模型调用；旧历史记录保持为空而不是回填猜测值。

- [completed] 1. 新增 Flyway V3：为 recommendations 增加可空审计列 model_name、prompt_version、provider_call_id、prompt_tokens、completion_tokens、total_tokens、generation_latency_ms、fallback_reason，兼容 H2/PostgreSQL 与旧 V2 数据。
- [completed] 2. 百炼客户端解析真实响应 id、model 与 usage 三类 token，定义稳定 prompt 版本常量，缺失值不伪造。
- [completed] 3. 服务层保存稳定枚举式 fallback 原因，不持久化异常原文；生成耗时覆盖天气+模型+规则选择全流程且非负。
- [completed] 4. API 通过嵌套 generationAudit 返回元数据，旧前端只读 engine 保持兼容。
- [in_progress] 5. 测试：合法 LLM 元数据落库与返回、无 key/异常/非法 ID/同类别 fallback 原因、V2->V3 迁移保留旧数据且新列为空、usage 缺失时成功但 token 为空。
- [pending] 6. 同步 README、llm-integration-evidence、development-boundaries、findings、progress 与收集核对结果。

## 阶段九执行记录

- 前两次 Claude CLI 批处理均因 8 美元预算上限退出；第一次未产生任何文件修改，第二次已完成配置口径改动（Actuator 收敛、移除 pgvector/Prometheus 口径、固定前端 vite 版本）。本轮改为小批次逐项落地并逐项验证。

## 阶段九离线天气演示完成结果

- 新增默认关闭的开关 `app.weather.configured-demo-enabled` / `WEATHER_CONFIGURED_DEMO_ENABLED`（默认 `false`），以及静态快照字段 city、temperatureC、apparentTemperatureC、precipitationMm、weatherCode、windSpeedKmh。
- 仅当两个真实 provider（wttr.in、Open-Meteo）都失败且请求城市与配置城市（trim 后不区分大小写）一致时，才返回静态快照，`source=configured-demo`；前端把该 source 明确标注为“配置演示天气/非实时”，不冒充实时天气。城市未找到（NOT_FOUND）不被静态快照掩盖，仍返回 404。
- 配置校验采用与现有缓存配置一致的 fail-fast 方式：启用时任一字段缺失/非有限/温度不合理/降水风速为负，应用启动失败。
- 测试：`WeatherServiceTest` 覆盖默认关闭双失败仍 503、开启且城市匹配返回 configured-demo、城市不匹配仍 503、NOT_FOUND 仍 404、以及配置非法的反例。
- 文档：application.yml、docker-compose.yml、.env.example、README、development-boundaries、findings、progress 已同步，明确答辩可在 `.env` 显式启用但不能冒充实时天气。

---

# 阶段十：审计缺口收尾执行

## 目标

在最外层审计任务中收尾两项可复现交付：① 默认离线、不调用付费模型的研究评估协议与脚本；② 局部修订毕业设计 DOCX 生成器并重新生成，使其措辞与当前源码/迁移/测试/配置一致。本阶段只追加记录，不改写既有历史。

## 已完成

- [completed] 1. 只读审计当前工作区：git status/diff、task_plan/findings/progress、后端 API/DTO/迁移/测试、前端契约、README 与 docs、build_graduation_doc.py。
- [completed] 2. 实现离线评估协议与脚本（scripts/evaluation/）：纯标准库 Python，默认只读本地 JSON 并计算，不联网、不调用百炼或任何模型 API，不内置实验结论。
- [completed] 3. 运行评估反例测试（25 项 unittest 全部通过）与 fixture 评估（输出真实计算指标）。
- [completed] 4. 局部修订 build_graduation_doc.py 并重新生成 docx/基于大语言模型的智能穿搭推荐系统_毕业设计成果.docx。
- [completed] 5. 结构 QA（ZIP/段落/表格/关键术语）通过；视觉渲染 QA 因环境缺失 pdf2image 与 LibreOffice 未完成，已记录确切失败信息。
- [completed] 6. 记录本阶段真实状态到 task_plan/findings/progress，并做最终差异检查。

## 验收条件

- 评估脚本默认离线，seed 实际控制抽样；输出只含真实计算结果或明确未执行状态。
- 反例覆盖缺字段、重复 ID、衣橱外 ID、同类别、空数据；规则引擎契约与 LLM 输入边界分开定义。
- DOCX 不再出现 JWT、Redis、管理员后台、pgvector、旧表名与“待实测”；改为 Session+CSRF、Flyway V1-V3、后端 62 项测试、4 项 E2E、推荐审计字段与离线演示天气边界。
- 记录只追加不重写；所有失败命令与原因写入记录。

---

# 阶段十一：当前版本交付审查与最小修复

## 目标

在不覆盖未跟踪用户文档的前提下，复核远端同步、依赖审计、后端事务一致性和可运行性；代码改动由 Claude 子代理实施，主代理负责文档同步与最终验证。

## 阶段

- [completed] 1. 确认 `codex/fix-major-gaps` 与远端同提交，并记录未跟踪项
- [completed] 2. 运行后端、前端、离线评估和 Compose 静态回归
- [completed] 3. 修复 `nanoid` 高危依赖；Claude 已为图片上传增加事务边界和补偿清理保护，并修复天气 NOT_FOUND 组合降级语义
- [completed] 4. 同步 README、开发边界、答辩/评估口径及规划记录
- [pending] 5. 在 Docker 可用后重跑完整栈 E2E；不可用时保留明确环境限制
- [completed] 6. 最终检查远端差异、工作区和剩余风险

## 当前反例

- `npm audit --audit-level=high` 已通过，`nanoid` 解析为 3.3.18；当前直接依赖声明略宽于传递依赖修复的最小范围。
- `WardrobeService.upload` 已加入 `@Transactional`，对象补偿删除失败不会覆盖原始数据库异常；仍缺少 `updateImageUrl` 失败时的独立回滚测试。
- `WeatherService` 已覆盖主 provider `NOT_FOUND`、备用错误且配置演示开启的 404 反例。
- `docker compose ps` 当前无法连接 Docker Desktop Linux Engine，真实 E2E 尚未重跑。

---

# 阶段十二：2026-09-07 审查问题修复

## 目标

修复本轮五项审查问题，保留已有未提交业务代码和文档，验证实际请求与本地完整栈。

- [completed] 1. 核对当前配置、调用链、既有改动和本地 PostgreSQL 镜像 digest
- [completed] 2. 后端 Secure Cookie 默认开启，并为本地 HTTP 显式配置 false
- [completed] 3. 限制历史查询偏移量为 1,000,000，使用 long 运算，补边界与整数最大值测试
- [completed] 4. 固定 PostgreSQL digest，统一 Node engines 和部署文档
- [completed] 5. 执行后端、前端、离线评估、Compose 和 E2E 验证
- [completed] 6. 复核差异并记录实际结果

## 阶段十二验证结果

- Maven 12 个测试类、66 项测试通过，0 失败/错误/跳过；含 Cookie 配置、分页整数最大值、偏移边界及 hasNext 上限反例。
- npm ci、生产构建与依赖审计通过，0 vulnerabilities；Python 离线评估 38 项通过。
- Compose 配置、前后端镜像重建及完整栈启动通过，后端 health 为 UP。
- PostgreSQL 使用固定 digest 启动，与原容器镜像一致；PostgreSQL/MinIO 原命名数据卷保留。
- Playwright 4/4 通过，应用地址 http://localhost:8090。付费模型开关保持关闭。
- 保留原有未提交业务和 DOCX 改动，未暂存、提交或推送；未执行生产 HTTPS 部署验证。

---

# 阶段十三：网页内容采集接入趋势界面（2026-09-08）

## 目标

参考用户提供的 SnapWC“当前页面识别公开资源”的思路，为趋势页增加受控的公开网页内容采集适配器；只采集管理员配置的授权 URL，抽取标题、摘要、标签、时间、封面和来源链接，标准化为现有趋势契约，并在无法采集时继续明确降级到开发样本。

## 第一性边界

- SnapWC 是媒体下载解析器，不提供可直接复用的时尚趋势 API；本项目只复用“读取公开页面并提取结构化元数据”的思路，不调用或仿造其内部接口。
- 任意网页无法可靠提供平台真实热度；网页适配器的分数只能是“来源页信号评分”，必须在接口/界面中区别于授权源热度。
- URL 来自服务端配置而非前端用户输入，并拒绝明显的本地/内网地址，避免把趋势采集变成 SSRF 代理。

## 阶段

- [completed] 1. 新增网页采集适配器与 HTML/JSON-LD 标准化
- [completed] 2. 扩展趋势字段契约与前端显示，区分来源页信号评分并展示摘要
- [completed] 3. 补充配置、文档和反例测试
- [completed] 4. 执行后端/前端/Compose/趋势采集回归并记录限制

## 阶段十三完成结果

- 新增 `ConfiguredWebTrendSourceAdapter`：读取服务端配置的多个公开 HTTP(S) 页面，解析 OpenGraph、JSON-LD、标题、`article` 卡片、摘要、标签、时间、图片和来源链接；支持单页多文章、部分页面失败保留成功结果和 Caffeine 缓存。
- 趋势条目新增可选 `summary`，趋势响应新增 `scoreLabel`；授权 JSON 仍兼容旧字段，网页源明确返回“来源页信号评分”，前端首页、风潮页和个人档案页同步显示该口径。
- 新增 `TREND_WEB_URLS`、网页平台、单页大小和单页文章数配置；默认空值不发起网页请求，原 JSON 优先，全部源不可用仍返回开发样本。
- 新增网页采集适配器 7 项测试，覆盖 HTML/JSON-LD、列表文章、缓存、全部失败、部分失败、非法 URL、缺失可选元数据；后端总计 75 项测试通过，前端构建和 Compose 配置检查通过。
- 本轮未配置具体时尚趋势网页 URL，未进行真实第三方趋势页面联调；SnapWC 首页仅作为能力边界参考，不被写入趋势源配置。

## 对抗性用例

- 空配置时不发起网页请求，接口仍返回 demoMode=true。
- 页面缺少 JSON-LD、标签、日期或图片时仍能生成最小合法趋势条目，但不伪造平台热度。
- 页面返回无效日期、非 HTTP(S) 图片、重复文章或超限内容时，不把脏结果标记为实时趋势。
- 配置 localhost、回环地址、内网地址或非 HTTP(S) URL 时拒绝采集。
- 配置的多个页面中部分失败时保留成功页面；全部失败时统一走开发样本降级。

## 本轮恢复错误

- 初次读取聚合输出截断，改为按路径和关键行读取。
- 猜测的 SecurityConfig.java 与 E2E spec 路径不存在，改用 rg --files 定位。

---

# 阶段十四：风潮 Top 10 三维弧形画廊（2026-09-08）

## 目标

在风潮界面增加一个可操作的 3D 弧形画廊，展示按热度降序排列的 10 套风潮穿搭。当前抓取功能尚未开发，因此用后端开发样本提供 10 条稳定数据；未来真实趋势适配器只需继续返回现有 TrendItem 契约即可替换样本。

## 第一性边界

- 首页负责快速理解和进入任务，风潮页负责探索；弧形画廊的注意力和交互成本只放在风潮页。
- “Top 10”必须由数据排序得到，而不是把 10 个展示位硬编码成视觉顺序；接口接入后仍按 heatScore 排序。
- 当前没有真实抓取热度，开发样本必须继续通过 demoMode 标记，不把本地占位数据冒充线上热度。
- 3D 只是浏览层，不改变趋势筛选、来源链接和现有趋势详情契约；必须提供键盘/按钮操作、移动端降级和 reduced-motion 支持。

## 阶段

- [completed] 1. 扩充开发样本到 10 条并更新趋势契约测试
- [completed] 2. 在 TrendView 实现 Top 10 3D 弧形画廊、详情联动和移动端交互
- [completed] 3. 执行前端构建、后端趋势测试和浏览器/静态对抗验证
- [completed] 4. 同步 findings、progress 和交付边界说明

## 对抗性用例

- 趋势源返回超过 10 条时，画廊仍只展示热度最高的 10 条。
- 热度相同、图片缺失或图片加载失败时，画廊仍能稳定渲染并保留来源/标题信息。
- 只有 0～2 条趋势时，3D 计算不会出现除零、越界或横向溢出。
- 用户快速点击下一套、切换标签或刷新数据时，选中项和详情不会脱离当前数据集。
- `prefers-reduced-motion: reduce` 下不依赖动画才能访问全部条目。

## 阶段十四完成结果

- `TrendService` 开发降级 feed 已扩充为 10 条，`TrendServiceTest` 与 `TrendControllerTest` 已同步验证数量；接口继续返回 `demoMode=true` 和 `scoreLabel=开发样本热度`。
- `TrendView` 新增按 `heatScore` 降序取前 10 的弧形画廊，中心卡片抬高、两侧逐级下沉并旋转；卡片、左右按钮、数字 tab、方向键、Home/End 均可浏览并联动详情。
- 桌面截图与用户参考图完成视觉复核；390px 移动端确认画廊仍保留 10 个条目、舞台高度 380px，页面 `scrollWidth` 未超过布局宽度。
- `npm run build` 通过；Maven 全量测试 75/75 通过；运行栈接口返回 10 条热度降序样本；浏览器回归 0 个页面错误，仅有已知的地理位置权限警告。
- `LoginView` 同步修正 Chromium `v` 正则模式下的连字符转义，避免浏览器控制台报无效 pattern。

## 本轮过程错误

- PowerShell 初次传入 Maven 多测试类参数时被逗号解析，改为对 `-Dtest` 参数加引号后通过。
- Playwright Bash 包装脚本依赖本机不可用的 WSL bash，改用同版本 `npx @playwright/cli` 完成验证。
- 浏览器首次复用旧 URL 缓存了旧 bundle，使用带版本查询参数的 URL 后重新确认最新画廊样式。

---

# 阶段十五：风潮画廊视觉与自动轮播精修（2026-09-08）

## 目标

根据新的参考图和明确反馈，收敛画廊为无独立底色的图片展示区：调整标题、移除位次/箭头/底部说明/01–10 导航，拉开图片间距并增加自动轮播。

## 第一性边界

- 画廊的核心价值是展示穿搭图片，辅助文字和多组微型控件不应与图片争夺注意力；删除它们不影响趋势详情和数据契约。
- 自动轮播只移动画廊焦点，不自动改写页面其他趋势详情；用户主动点击卡片时仍可联动原有趋势详情。
- 透明舞台保留页面自身背景，间距根据卡片宽度计算，避免在大屏和移动端分别硬编码后产生重叠或溢出。

## 阶段

- [completed] 1. 重写标题与视觉层级，移除 `01 / 10`、箭头、底部说明和 01–10 导航
- [completed] 2. 取消独立浅色舞台背景并按舞台宽度拉开卡片间距
- [completed] 3. 增加带生命周期清理、悬停/聚焦暂停和 reduced-motion 保护的自动轮播
- [completed] 4. 完成桌面/移动端截图、交互、构建和设计 QA

## 对抗性用例

- 390px 移动端在拉开间距后仍不能出现根布局横向溢出或卡片重叠。
- 自动轮播不能因页面隐藏、鼠标悬停、键盘聚焦或组件卸载而继续改变状态。
- 自动轮播不应让页面下方原有趋势详情随时间跳动；主动点击卡片仍应能选中趋势。
- `prefers-reduced-motion: reduce` 下自动轮播关闭，图片仍可通过卡片和键盘访问。

## 阶段十五完成结果

- 标题改为“风潮穿搭精选”，移除 `gallery-position`；移除箭头、底部焦点文字、提示语和数字导航。
- `.gallery-stage` 改为透明舞台；桌面卡片间距实测约 30–46px，移动端约 22–39px。
- 自动轮播每 1 秒前进一次，仅更新画廊索引；鼠标悬停、键盘聚焦、页面隐藏和组件卸载均会暂停/清理定时器。
- `npm run build`、Docker 前端镜像重建、桌面 1440px 和移动 390px 浏览器回归通过；页面 0 个错误。

## 本轮过程错误

- 初次仅增加桌面偏移时，390px 截图发现移动卡片仍有明显重叠；改为按移动卡片实际宽度加固定间隙计算偏移后重新构建并通过。

---

# 阶段十六：自动轮播节奏与图片悬停暂停（2026-09-08）

## 目标

将风潮画廊自动轮播调整为每 1 秒切换一次，并确保鼠标悬浮在任意图片卡片上时暂停，移出图片后恢复。

## 阶段

- [completed] 1. 将自动轮播间隔改为 1000ms。
- [completed] 2. 使用图片卡片级事件委托实现悬停暂停与移出恢复，卡片之间移动时保持暂停。
- [completed] 3. 保留键盘聚焦、页面隐藏、组件卸载和 reduced-motion 的暂停/清理保护。
- [completed] 4. 完成构建、容器健康状态和浏览器交互回归验证。

## 对抗性用例

- 无悬停等待 1.3 秒，当前图片应发生变化。
- 悬浮当前图片等待 1.3 秒，当前图片应保持不变。
- 移出图片后等待 1.3 秒，当前图片应恢复变化。

## 阶段十六完成结果

- `TrendView.vue` 使用 `setInterval(..., 1000)`；自动轮播仍只更新画廊索引，不联动改写趋势详情。
- 浏览器回归确认悬浮时前后图片标题一致，移出后图片标题发生变化；页面控制台无错误。

## 本轮过程错误

- 分开执行浏览器悬停和等待命令时，1 秒轮播会在命令间隔内先后移动焦点，造成一次假阴性；改为在单次浏览器脚本中完成“悬浮—等待—读取”事务后通过。

---

# 阶段十七：登录/注册衣料缝线切换动画（2026-09-09）

## 目标

在不改变认证契约和现有克制视觉的前提下，完成登录/注册 Tab 与内容切换的衣料缝线动效：激活 Tab 使用 2px 金棕缝线并带小圆点滑动，标题/辅助文案上下淡入淡出，注册确认密码层叠展开，提交按钮位置稳定，切换后聚焦当前状态首个可编辑字段；同时补齐移动端媒体区域与 reduced-motion 降级。

## 第一性边界

- 必须解决的是状态切换的可感知性和表单可用性，不是增加装饰；动画总时长控制在 220–280ms，不能阻塞输入。
- 登录与注册仍是文字 Tab，不改成胶囊按钮；认证请求、校验、错误提示和现有字段命名不改变。
- 确认密码是注册状态独有字段，但它的出现不能推动提交按钮大幅跳动；使用稳定的内容区/提交区布局而不是绝对定位遮挡输入。
- 当前仓库没有可用视频素材；本轮提供 video/poster 的兼容结构和响应式封面降级，不伪造或引入不存在的视频资源。
- reduced-motion 下保留状态可达性、焦点和错误反馈，关闭缝线/内容位移动画与旋转装饰。

## 阶段

- [completed] 1. 审计 LoginView 当前 DOM、表单契约、响应式断点与可用媒体素材
- [completed] 2. 实现 Tab 缝线、内容切换、确认密码展开和首字段自动聚焦
- [completed] 3. 实现输入聚焦/按钮 hover/错误边线及移动端媒体降级
- [completed] 4. 构建并用桌面、移动端和 reduced-motion 对抗用例验证
- [completed] 5. 同步 findings、progress 与交付边界说明

## 阶段十七完成结果

- LoginView 完成 240ms 文字 Tab 缝线动画、确认密码层叠展开、固定提交按钮槽位、切换后首字段聚焦与快速切换禁用保护；后续按界面反馈移除重复的登录/注册标题文案。
- 右侧输入 focus ring、主按钮 hover 金棕边缘和珊瑚错误边线保持/增强；移动端 180–220px 静态视觉封面、矮屏隐藏媒体且不横向溢出。
- `npm run build` 已通过；Playwright CLI 已通过桌面/390×844/390×650、reduced-motion、按钮稳定、焦点、快速切换和悬停样式检查。
- 当前后端未运行，浏览器只记录既有 `/api` 502 安全会话请求；未产生 Vue/CSS 运行时错误。

## 阶段十七后续：移除重复认证标题（2026-09-09）

- 删除“欢迎回来 / 建立你的账户”与“登录知己 / 注册知己”两态标题，保留 Tab、表单和缝线/确认密码动效。
- 将认证面板改为显式 `aria-label="账户认证"`，同步移除失效标题 ID 与标题过渡 CSS。

## 对抗性用例

- 快速连续点击登录/注册时，不出现旧标题回写、焦点落到隐藏字段或提交按钮跳到视口外。
- 注册切到登录后确认密码必须从可访问树移除；登录切回注册后确认密码可编辑且校验仍生效。
- 切换后 300ms 内首个可编辑字段获得焦点；提交中点击 Tab 不触发状态或焦点竞争。
- 视口宽度 390px、高度 700px 时页面不横向溢出，媒体区不挤压表单；更矮屏幕自动隐藏媒体但保留品牌和功能。
- `prefers-reduced-motion: reduce` 下切换仍完成、字段仍聚焦、无依赖 CSS 动画才能显示内容。
- 图片或未来视频媒体加载失败时，使用封面色/静态封面，不显示破损媒体图标或影响认证表单。

# 阶段十八：毕业设计文档管理员模块与图表重排

## 目标

在保留官方模板版式和现有成果文档结构的前提下，补充系统管理员后台运营管理内容；重绘普通用户用例图；新增系统管理员用例图；把核心业务流程图改为企业级跨职能泳道流程；将用例分析表内的数字序号改为竖向排列，并完成文档渲染与逐页复核。

## 阶段

- [completed] 1. 读取模板与当前成果文档，区分模板示例与项目真实内容
- [completed] 2. 记录模板版式契约、页面系统和可编辑位置
- [completed] 3. 生成三张高分辨率图表并替换/插入到第 2 章
- [completed] 4. 补充管理员模块正文、目录标题和用例分析表
- [completed] 5. 将用例分析表中的编号步骤竖向排列
- [completed] 6. 通过 Word PDF/Poppler 渲染，逐页检查并修正分页与可读性

## 阶段十八完成结果

- 已生成并嵌入企业级四泳道核心业务流程图、普通用户用例图和系统管理员用例图；管理员图的段落属性已修正，Word 导出显示正常。
- 已补充 `1.2.7 系统管理员后台运营管理`、`2.3.4 系统管理员后台管理` 及 4 张管理员用例分析表（表 2.8—表 2.11）。
- 已将现有普通用户 7 张用例表和新增管理员用例表中带数字序号的基本路径、扩展路径、异常事件流逐项换行排列。
- 已用 Word 临时 PDF + bundled Poppler 渲染 43 页，并逐页检查；目录字段已更新，输出结构审计和 DOCX 压缩包完整性检查通过。

## 阶段十八对抗验证

- 普通用户图未加入管理员治理操作；管理员图未加入普通用户私有衣橱编辑和搭配生成操作。
- 流程图显式覆盖天气服务失败、候选不足、模型不可用/降级、结果校验失败和审计保存路径。
- 11 张用例表中存在数字序号的路径均已检测到换行标记；4 张管理员表的标题与表格顺序连续。
- 源成果文档 SHA-256 仍为 `C62B59BCB644C9EC7A51303FAC8C9CE8F895A7A6CF35F2E70F7F0D117C7150E4`，未被覆盖。

## 第一性边界

- 模板中的“机关单位、领导、商城”等段落是示例，不是本项目需求；不得迁移到穿搭系统。
- 管理员模块只描述平台级治理与运维边界：账号状态、授权趋势源、运行状态、脱敏审计和清理任务；不直接取得普通用户私有图片或替代普通用户生成搭配。
- 图表首先服务于可读性与业务关系表达；复杂装饰不替代 UML 角色/用例关系和流程决策语义。
- 用例表中序号的根本问题是同一行文字过密导致步骤不可扫描；使用换行分隔，不缩小字体或压缩列宽。

## 对抗性用例

- 普通用户用例图不能出现管理员专属操作；管理员图不能出现普通用户的私人衣橱编辑操作。
- 流程图在天气服务失败、模型不可用、候选不足或模型结果不合法时必须有可见的降级/异常路径。
- 管理员表格的基本路径、扩展路径和异常事件流的 `（1）`、`（2）` 等序号必须逐行显示。
- 新图在 A4 页面内嵌入后，中文标签、箭头、决策分支和图题不得裁切或重叠。
- 模板、原始成果文档和两份用户输入文件保持不被覆盖。

## 遇到的错误

| 错误 | 尝试次数 | 处理 |
| --- | --- | --- |
| 规范 `render_docx.py` 找不到工作区自带 `soffice.exe` | 1 | 定位到运行时未提供 LibreOffice；改用已安装 Word 的只读 PDF 导出与 bundled Poppler 光栅化，仅用于 QA，不保存回源文档 |
| Word `ExportAsFixedFormat` COM 参数返回 Value does not fall within expected range | 1 | 改用只写临时 PDF 的 `SaveAs2(..., wdFormatPDF)`，两份文档均成功导出 |

## 视觉/交互工作模型

- 视觉 thesis：像一根金棕缝线沿墨黑与暖白之间移动，宋体标题承载人的风格，无衬线表单维持清晰效率。
- 内容 plan：品牌先建立身份；Tab 说明“继续已有风格 / 开始新的风格记录”；标题与表单完成认证；媒体区域只承担衣料氛围，不抢表单任务。
- interaction thesis：激活 Tab 的缝线滑动建立状态连续性；标题组短促上下淡入淡出表达换页；确认密码以衣物层叠方式展开，同时稳定提交区减少页面跳动。

# 阶段十九：系统架构图按参考图重构

## 目标

在上一版管理员模块与图表优化成果的基础上，仅重构第 2.4.1 节系统总体架构图，使其严格遵循用户提供参考图的横向分层、左侧外部数据源、中心应用服务、底部基础设施与保障体系构图方式；项目内容只使用当前系统已实现或已在文档中明确设计的模块，不照搬参考图中的政务业务文字。

## 阶段

- [completed] 1. 读取参考图、现有第 2.4.1 图和项目实现事实，确认结构差异与内容边界
- [completed] 2. 按参考图重绘项目化系统架构图并进行图片级视觉检查
- [completed] 3. 替换成果副本中的架构图并同步 2.4.1 说明文字
- [completed] 4. 用 Word 导出 PDF、逐页复核分页/图题/图像清晰度并完成结构断言

## 验收条件

- 架构图保留参考图的横向多层布局、左侧第三方系统/数据源、中央应用服务与基础组件、数据访问层、服务器层、基础工具和底部三类保障体系。
- 普通用户与系统管理员入口、认证边界、天气/趋势/模型外部服务、衣橱/推荐/治理业务、数据库/对象存储/缓存等内容与当前项目一致。
- 2.4.1 说明文字与图中分层一致，图题仍为“图2.4 系统总体架构图”。
- 输出为新的成果 DOCX 副本，不覆盖原成果文档和上一版管理员模块成果。
- Word 导出后的页面无新增裁切、重叠、空白异常，图中文字可辨识，文档可正常打开。

## 阶段十九完成结果

- 已生成接近参考图比例的横向企业级架构图，保留参考图的多层横向排布、左侧第三方数据源和数据总线/接口服务、中央应用服务与基础组件、数据访问层、服务器资源、基础工具及三类保障体系。
- 已将项目实际的普通用户/系统管理员入口、Session 与权限认证、wttr.in/Open-Meteo、百炼 Qwen、授权趋势源、用户业务、管理员治理、PostgreSQL、MinIO 和 Caffeine 映射到图中。
- 已更新 2.4.1 说明文字，并在新的成果副本中替换图 2.4；源成果文档、上一版管理员模块成果和模板均未覆盖。
- Word 临时 PDF 导出显示 43 页；第 2.4 章分页稳定，图 2.4 完整呈现且图题紧随其后，2.4.2 后续内容正常承接；DOCX 结构与压缩包完整性断言通过。

## 阶段十九对抗验证

- 目录中存在同名 2.4.1 条目时，替换脚本必须只命中正文 Heading 3，不能改写目录文字或误替换前一张图；已用正文标题索引 179、图像段落索引 181 复核。
- 将新图嵌入 A4 页面后，图中文字、连接箭头、左侧数据源、底部保障体系和图题均未裁切或重叠。
- 通过 SHA-256 复核确认原成果文档仍为 `C62B59BCB644C9EC7A51303FAC8C9CE8F895A7A6CF35F2E70F7F0D117C7150E4`，上一版管理员成果仍为 `380B064C7B8F5C16F7CFC47DCF0FFC3AFD82C15B47AD5316457C71EB790547B3`。

# 阶段二十：目录右侧页码统一对齐

## 目标

在上一版架构图按参考图成果副本的基础上，修正目录右侧页码没有统一对齐的问题。保留目录层级缩进和点引导线，仅把 TOC 1、TOC 2、TOC 3 的页码统一到同一个右对齐制表位；输出新的成果 DOCX 副本，不覆盖任何已有成果文件。

## 阶段

- [completed] 1. 读取目录参考图并检查最新 DOCX 的目录样式、制表位和字段结构
- [completed] 2. 统一 TOC 1/2/3 的右对齐点引导制表位
- [completed] 3. 写入新的成果副本、刷新 TOC/PAGE 字段并导出 PDF
- [completed] 4. 放大检查目录页、抽查正文页并完成结构/完整性审计

## 验收条件

- 所有目录层级的页码右边缘落在同一条右边界，点引导线连续且不改变层级缩进。
- 目录页码与最新正文分页一致，TOC 字段已刷新。
- 输出只修改目录格式，架构图、管理员模块、用例图、流程图和表格等上一轮内容保持不变。
- 新输出 DOCX 可打开、压缩包完整，并通过 Word 导出后的页面视觉检查。

## 阶段二十完成结果

- 已删除 TOC 1 的旧 `8777` twips 右对齐制表位，并让 TOC 1、TOC 2、TOC 3 均只使用 `9360` twips 的右对齐点引导制表位；左侧层级缩进保持不变。
- 已生成新的成果副本并用 Word 刷新 TOC/PAGE 字段；目录第 3—4 页的页码已统一右对齐，点引导线连续。
- Word 导出 PDF 共 43 页；全篇接触表和目录放大检查通过，架构图、管理员模块、用例图、流程图及表格未出现新增分页或版式异常。
- DOCX 压缩包测试通过，TOC 字段仍包含 71 个 PAGEREF，页码字段仍包含 5 个 PAGE；最终输出未覆盖旧架构图成果副本。

## 阶段二十对抗验证

- 目录一级、二级、三级条目混排时，页码右边缘一致；长标题“2.4.4 方法、参数、成员变量、局部变量命名规则”也未使页码换行或越过右边界。
- 目录字段刷新后页码与正文实际分页一致，2.4.1 与 2.4.2 均显示为第 17 页。
- 最终输出 DOCX `word/styles.xml` 中 TOC 1/2/3 均只有一个 `right + dot + 9360` 制表位，压缩包 `testzip` 结果为 OK。

# 阶段二十一：推荐页 AI 穿搭助手工作台（2026-09-14）

## 目标

把推荐页中的“按条件生成搭配”从普通表单改成上下文式 AI 穿搭助手：保留现有推荐 API 契约和今日搭配主内容，以自然语言输入、快捷意图、上下文摘要和结构化结果卡降低填写成本；桌面端使用侧边面板，移动端使用底部抽屉。

## 第一性边界

- 推荐页的核心任务仍是帮助用户快速决定今天穿什么，AI 助手是生成和追问入口，不应抢走今日主推荐。
- 后端当前只接受 `occasion`、`city`、`styleHint`；本轮不伪造多轮聊天接口，助手提交时复用现有 `generateRecommendation`，自然语言作为风格/场景提示进入现有契约。
- 空衣橱或没有真实推荐记录时，不能把固定视觉方向包装成真实可穿搭配；界面明确区分“灵感方向”和“衣橱推荐”。
- 助手状态必须可见：打开、输入、生成中、成功、失败、关闭后再次打开均有明确反馈；键盘和移动端触控路径保持可用。

## 阶段

- [completed] 1. 将推荐页生成区改为 AI 助手入口、对话面板和快捷意图交互
- [completed] 2. 调整今日搭配/天气/衣橱信息层级，消除重复与语义冲突
- [completed] 3. 完成桌面、移动端、空数据、生成中、失败和键盘交互验证
- [completed] 4. 同步 findings、progress，构建并提交推送本轮设计改动

## 验收条件

- “问问知己”能在不跳转页面的情况下打开助手；桌面右侧面板、移动端底部抽屉均有关闭和焦点可达路径。
- 自然语言输入、快捷提示和现有场合/城市/风格条件能共同生成真实推荐，成功后结果在助手中可见并保留当前收藏/评分闭环。
- 生成中、错误、空衣橱状态不伪造成功；错误保留用户输入并给出下一步。
- 真实推荐结果与灵感方向有明确文案区分；查看非当天日期时天气说明不把当前天气伪装成未来/过去预报。
- `npm run build` 通过；桌面与 390px 移动端无根布局横向溢出，助手控件具备可见焦点。

## 当前实现决策

- 默认收起的助手入口放在今日搭配区域附近，主按钮使用“问问知己”；助手内容通过页面内 panel/drawer 呈现。
- 快捷意图包括通勤、约会、周末、这件怎么搭和更正式一点；提交前以当前天气/城市/衣橱数量作为上下文提示。
- 生成成功后助手消息展示摘要、衣物和推荐理由，并提供“换一套”“更正式”“少穿一层”“收藏这套”等后续操作；后续操作复用现有推荐接口。

## 遇到的错误

| 错误 | 尝试次数 | 处理 |
| --- | --- | --- |
| 规划技能首次读取使用了错误的 `.codex` 根路径 | 1 | 根据技能根目录表改用 `C:\\Users\\jaime\\.agents\\skills\\planning-with-files-zh\\SKILL.md`，成功读取 |
| 管理员用户查询在 H2 执行 LIKE 时提示 `Error in LIKE ESCAPE` | 1 | Java SQL 字符串把反斜杠转义多写了一层；改为 SQL 使用单个反斜杠作为 ESCAPE 字符，并重跑针对性测试 |
| 全量后端回归中认证集成测试得到默认 CSRF header `X-CSRF-TOKEN` | 1 | 管理员测试通过但完整测试上下文组合出现配置差异；检查 CSRF 配置/测试上下文后再修复，保持 `X-XSRF-TOKEN` 契约 |
| CSRF 差异在单独运行认证测试时未复现 | 1 | 已确认单类 `AuthenticationIntegrationTest` 通过，继续用管理员测试在前的最小顺序复现共享上下文状态；若确认是测试污染则隔离测试，不放宽生产 CSRF |
| PowerShell 直接传入带逗号的 `-Dtest` 参数触发解析错误 | 1 | 将完整 Maven 系统属性用引号包裹后重试，不改变测试内容 |
| Vite 在本地端口 `5174` 启动时返回 `EACCES` | 1 | 改用项目原型约定的 `5188` 端口进行浏览器验收，不重复占用被系统拒绝的端口 |
| Vite 在本地端口 `5188` 启动时仍返回 `EACCES` | 1 | 停止尝试新 Vite 端口，改用已有 `8090` 容器重建路径进行验收 |
| Playwright 包装脚本依赖的 WSL `/bin/bash` 不存在 | 1 | 改用同一 `@playwright/cli` 的 `npx` 入口，不重复调用包装脚本 |

## 阶段二十一完成结果

- 推荐页保留今日搭配为主任务；原三字段生成表单改为“问问知己”入口，桌面为右侧抽屉，390px 移动端为底部抽屉。
- 助手支持自然语言、通勤/周末/正式/这件怎么搭快捷意图、地点/天气/衣橱/风格上下文摘要；提交复用现有 `occasion`、`city`、`styleHint` 契约，不新增虚假聊天后端。
- 结果卡展示引擎、场合、城市、摘要、衣物缩略图、推荐理由，并保留换一套、更正式、少一层和收藏操作；与现有生成结果、收藏、评分、历史闭环联动。
- 推荐页明确区分“来自衣橱”和“灵感方向”，默认优先真实衣橱搭配；非当天日期显示“计划参考”，不把当前天气包装成未来/过去实时预报；空状态文案改为引导使用助手。
- 已通过 `npm run build`、`impeccable detect`（无问题）和前端全量 E2E 6/6；主链路测试覆盖自然语言生成、收藏、评分和历史持久化。
- 浏览器检查通过：1440px 抽屉宽 460px、高度 900px，打开后焦点落在输入框、`body` 锁滚动，Escape 可关闭；390px 底部抽屉无根布局横向溢出。

## 阶段二十一对抗验证

- 新用户无推荐记录时只显示“灵感方向”，不能把固定的五个视觉模板计入衣橱搭配数量；有推荐记录时卡堆默认将真实衣橱搭配置于顶层。
- 助手提交不含城市时会停在对话内提示补充城市，不发出不完整推荐请求；后端生成失败时保留用户消息并显示可继续操作的错误提示。
- 打开助手后按 Escape 或点击遮罩均可关闭，关闭后页面滚动恢复；移动端抽屉高度受视口限制，输入、快捷意图和关闭按钮均在视口内。
- E2E 先用普通开发配置发现模型调用返回 `request-failed`，随后切换项目定义的 E2E Compose 覆盖配置（`BAILIAN_ENABLED=false`）复跑，确保测试审计断言为 `llm-disabled` 且 6/6 通过。

# 阶段二十二：管理员运营功能第一批（2026-09-14）

## 目标

在现有 Session + CSRF + `app_authorities` 认证基础上，落地管理员运营管理的第一个可运行闭环：管理员概览、用户账号分页查询和启用/停用治理，并提供受角色保护的管理端页面入口。

## 第一性边界

- 现有 `app_users.enabled` 与 `app_authorities` 已能表达账号状态和角色；先复用它们，不为管理员功能新造用户表或权限抽象。
- 管理员后台只能返回脱敏账号元数据和聚合统计，不读取普通用户私有衣橱、图片、推荐内容，也不替代普通用户生成搭配。
- 管理权限必须由服务端根据已认证 Principal/authority 强制执行；隐藏前端入口不是安全边界。
- 停用账号应立即影响后续登录；当前管理员不能停用自己，避免把唯一管理入口锁死。数据库更新必须带目标用户名条件，并对不存在目标返回 404。
- 概览统计采用聚合 SQL 并保持明确的响应上限；用户列表使用分页边界，不能返回无界账号集合。

## 阶段

- [completed] 1. 审计认证响应、权限配置、数据库表和前端路由，确定最小管理员契约
- [completed] 2. 增加管理员仓储/服务/控制器与角色保护，完成用户状态治理
- [completed] 3. 增加后端认证、越权、分页、停用即时生效和自停用保护测试
- [completed] 4. 增加管理端概览/账号页面入口，普通用户不可见且不可访问
- [completed] 5. 完成后端、前端、Compose 和浏览器对抗验证，并同步管理员实现边界文档

## 当前实现契约

- `GET /api/v1/admin/overview`：返回用户、启用账号、停用账号、衣物和推荐的聚合计数。
- `GET /api/v1/admin/users?page=&size=&query=`：返回脱敏账号列表、角色和启用状态，默认 20、最大 50，支持用户名查询。
- `PUT /api/v1/admin/users/{username}/status`：请求体只包含 `enabled`；管理员不能停用当前自己，目标账号不存在返回 404。
- 前端仅在 `/api/v1/auth/me` 返回管理员 authority 时显示管理入口；API 仍由 Spring Security 的管理员角色匹配保护。

## 对抗性用例

- 普通用户携带有效 Session 请求任一 `/api/v1/admin/**` 返回 403，直接访问管理页面也不能拿到管理数据。
- 未认证请求返回 401；伪造 `X-User-Id` 等旧客户端标识不能提升权限。
- 管理员停用普通用户后，该用户下一次登录失败；重新启用后可登录。
- 管理员尝试停用自己返回 400，且自己的账号仍为启用状态。
- 用户列表 `size=0`、超过最大值、负页码和超大偏移返回标准 400；查询结果不泄漏密码哈希。
- 概览接口只返回计数，不包含衣物名称、图片地址、推荐摘要或模型调用原文。

## 遇到的错误

| 错误 | 尝试次数 | 处理 |
| --- | --- | --- |
| 规划技能首次读取使用了错误的 `.codex` 根路径 | 1 | 根据技能根目录表改用 `C:\\Users\\jaime\\.agents\\skills\\planning-with-files-zh\\SKILL.md`，成功读取 |
| 管理员测试中的 H2 `LIKE ESCAPE` 多转义一层 | 1 | 修正 Java SQL 字符串，使 SQL 使用单个反斜杠作为 ESCAPE 字符 |
| 管理员 CSRF 测试污染共享 Spring Security 上下文 | 1 | 方法级隔离确认来源后，仅给管理员测试类加 `@DirtiesContext(AFTER_CLASS)` |
| 临时 UI 账号初次写入时 PowerShell 插值 BCrypt `$` | 1 | 用单引号保存哈希变量，更新临时账号并校验哈希长度 60 |
| 临时账号清理查询误写不存在的 `image_cleanup_outbox` 表 | 1 | 查询未执行删除；根据现有 schema 改查并精确清理 `style_profiles`、authority 和用户 |
| Playwright CLI 的 `run-code` 与 PowerShell 引号不兼容 | 1 | 改用页面内 `eval` 返回 Promise 等待动画，不再重复失败调用 |
| 扩展 `AdminOverview` 时删除了仍被 `countUsers` 使用的统计辅助方法，导致首次 Maven 编译失败 | 1 | 恢复无状态 `count(String)` 辅助方法后重跑编译 |
| 审计日志计数复用了完整列表 SELECT，导致 `COUNT` 读取 8 列 | 1 | 将计数和分页改为共享条件、分别构造单列/完整列 SELECT |

## 阶段二十二当前结果

- 后端已增加管理员概览、脱敏账号分页查询和账号启用/停用接口；`/api/v1/admin/**` 由服务端 `ROLE_ADMIN` 保护。
- `/api/v1/auth/login` 与 `/api/v1/auth/me` 现在返回 authority，前端据此显示管理入口；普通注册仍固定为 `ROLE_USER`。
- 管理页已接入现有单页路由，包含平台聚合指标、用户名搜索、分页、账号状态切换和当前账号保护；不展示密码哈希、私有衣橱、图片或推荐正文。
- 已加入本地演示 `demo-admin` seed；本次已按用户请求直接写入当前本地数据库，创建并启用 `demo-admin`，保留原有 `demo-user` 数据，未执行会重置 `demo-user` 的完整 seed。

## 阶段二十二验证结果

- `mvn -q -f backend/pom.xml test`：82 项通过，0 失败、0 错误、0 跳过。
- `npm run build`：通过。
- `docker compose config -q`：通过。
- 管理员针对性测试覆盖匿名/普通用户/伪造 `X-User-Id`、聚合与脱敏、分页边界、状态停用影响登录、重新启用、自停用保护、未知账号 404，以及管理员 authority 登录回传。

## 阶段二十二收尾说明

- 应用容器已重建到本轮代码；管理员真实登录、管理入口、概览、账号搜索和当前账号保护已通过浏览器验收；普通用户从 `#admin` 登录后已自动回到 `#home`。
- 当前数据库已存在可登录的 `demo-admin`（`ROLE_ADMIN`），临时 UI 账号及其空档案已清理；本次未执行完整 demo seed，因此 `demo-user` 原有数据保持不变。

# 阶段二十三：管理员运营后台第二批（2026-09-14）

## 目标

把管理员从“普通用户页面上多一个入口”升级为独立的运营工作台：保留共享认证与用户端能力，但进入管理区后提供数据概览、账号与权限治理、推荐/反馈运营、系统状态和操作审计，并用数据库迁移保存必要的治理记录。

## 第一性边界

- 管理后台的每个模块必须对应当前系统真实存在的数据或运行状态；没有公共服装库就不伪造“服装目录管理” CRUD。
- 管理员默认只看到脱敏聚合、账号元数据、反馈处理状态和模型调用审计，不直接读取普通用户私有衣橱、图片或推荐正文。
- 前端导航只是工作入口，所有管理接口仍由服务端 `ROLE_ADMIN` 保护；状态变更、反馈处理和权限调整必须记录操作者、对象和结果。
- 数据库结构只通过新增 Flyway 迁移演进，不能修改已执行迁移或用完整演示 seed 覆盖现有用户数据。
- 先完成可用的 P0 闭环（概览、账号、反馈、审计），模型配置、批量导入导出和多级角色保留为后续扩展，不制造无后端支撑的死菜单。

## 阶段

- [completed] 1. 审计现有衣物、推荐、反馈和审计字段，确定最小迁移与接口契约
- [completed] 2. 新增治理审计/反馈处理数据结构及后端管理接口
- [completed] 3. 建立独立管理工作台布局，接入概览、账号、运营和审计页面
- [completed] 4. 更新演示数据、README、数据库说明和相关技术栈文档
- [completed] 5. 完成后端/前端/数据库/Compose/浏览器对抗验证

## 计划验收条件

- 管理员进入管理区默认落在概览页，普通用户看不到管理导航且访问管理接口返回 403。
- 概览显示真实用户、衣物、推荐、反馈、平均评分、LLM 成功/降级和待处理反馈数据；无数据时明确显示空状态。
- 账号页支持搜索、分页、启停账号和角色展示；任何状态变更都有审计记录，不能停用自己。
- 反馈页支持查看脱敏反馈、按状态筛选和标记处理；处理人和处理时间可追溯。
- 审计页支持分页查看管理操作，不返回密码哈希、私有图片、推荐正文或认证密钥。
- Flyway 在 PostgreSQL/H2 可重复执行，旧数据保留；后端测试、前端构建、Compose 校验和真实浏览器路径通过。

## 阶段二十三第 1 项决策结果

- 管理概览使用 `app_users`、`wardrobe_items`、`recommendations`、`recommendation_feedback` 和 `image_cleanup_tasks` 的聚合数据，并增加平均评分、LLM/规则降级、待审核反馈、待人工识别和图片清理队列等真实指标。
- `recommendation_feedback` 增加 `moderation_status`、`handled_by`、`handled_at`，用户重新提交反馈时重置为 `PENDING`；管理员处理动作写入审计日志。
- 新增 `admin_audit_logs` 保存账号状态和反馈处理等管理写操作；日志不保存密码、图片、推荐正文、CSRF 或模型密钥。
- 不新增公共服装目录表，因为当前仓库没有公共服装数据来源；后台用概览中的数据质量指标代替不存在的内容管理能力。

## 对抗性用例

- 普通用户伪造管理员路由、请求参数或旧 `X-User-Id`，都不能读取管理数据或写入治理状态。
- 管理员重复点击状态/反馈操作、刷新后重试，结果不产生不可追踪的半写入状态。
- 反馈评论为空、超长、包含极端字符或对应推荐不存在时，接口返回明确 400/404 而不是 500。
- 审计对象用户名包含 `%`、`_`、反斜杠或不存在时，查询/记录仍保持正确且不泄漏其他对象。
- 零衣物、零推荐、零反馈和全是降级推荐的数据库状态下，概览仍能渲染，不显示虚假的成功率。

## 阶段二十三实现与验证结果

- 已新增 Flyway V4 `admin_governance`：反馈审核状态、处理人/时间、索引，以及 `admin_audit_logs` 审计表；旧迁移未修改。
- 已新增管理员反馈分页、反馈状态处理、审计日志分页接口，并把账号状态/反馈处理写入审计；服务端继续使用 `ROLE_ADMIN` 保护所有 `/api/v1/admin/**`。
- 已将用户重新提交反馈的语义设为重新进入 `PENDING`，避免管理员对旧版本反馈的处理状态覆盖当前内容。
- 已将管理页改为独立工作台：运营总览、账号与权限、反馈审核、操作日志四个分区；保留品牌外壳，但不复制普通用户的业务页面。
- 管理概览使用当前 PostgreSQL 真实数据：账号、衣物、推荐、反馈、评分、收藏、LLM/规则降级、待审核、待人工识别和图片清理队列；没有公共服装库，因此没有虚假的目录 CRUD。
- README、开发边界和系统流程图说明已同步到 V4 和当前 P0 管理闭环；演示 seed 只补充 `demo-admin`，未用完整 seed 覆盖现有 `demo-user` 数据。
- `mvn -q -f backend/pom.xml test`：84 项通过，0 失败、0 错误、0 跳过；`npm run build`：通过；`docker compose config --quiet`：通过。
- 容器已用最新 backend/frontend 镜像重建并健康启动；运行库 Flyway 版本为 v4，`demo-admin` 为启用的 `ROLE_ADMIN`，`demo-user` 为启用的 `ROLE_USER`；现有业务数据计数为 132 个账号、13 件衣物、31 条推荐、17 条反馈。
- 真实浏览器已验证管理员登录后进入 `#admin`，四个管理分区可切换，账号列表和脱敏反馈可见，移动宽度 390px 的页面菜单可用；普通用户登录和直接输入 `#admin` 均停留在 `#home` 且不显示管理入口。
- 浏览器检查发现登录页账号 `pattern` 在 Chromium Unicode Sets 规则下的连字符兼容问题，已做最小转义修复并重新构建；剩余初始化 `/auth/me` 的 401 属于未登录探测，不影响认证流程。

## 阶段二十三收尾边界

- 本轮完成的是有真实数据支撑的 P0 运营闭环；公共服装目录、密码重置、多级角色编辑、批量导入导出和真实基础设施监控仍需后续需求与数据源，不在本轮伪造入口。

# 阶段二十四：真实穿搭趋势与衣橱推荐（2026-09-15）

用户授权自主实现多平台日/周热门穿搭、流行风格和衣橱关联，自行查找工具。保留已有未提交管理员修改。
- [in_progress] 1. 核实真实来源、现有接口和运行环境，确定最小可运行采集契约。
- [pending] 2. 持久化趋势内容/快照，定时更新、多源合并、日周筛选、可追溯评分。
- [pending] 3. 推荐页/风潮页展示、来源与状态、风格选择及衣橱推荐联动。
- [pending] 4. 来源实测、后端反例测试、前端构建、浏览器桌面/移动验收。
- [pending] 5. 部署配置及文档，明确真实采集覆盖和待授权平台。
原则：不伪造全网热度；无历史快照不显示增长；失效保留最后真实数据；外部参考不能加入私有衣橱；无来源时显示空状态。

## 2026-09-15 管理员容器旧页面回归修复

- [completed] 1. 定位管理员会话恢复、前端静态资源和 Docker 镜像的实际状态。
- [completed] 2. 补齐登录完成、初始化和 hash 同步路径的管理员路由保护；普通用户仍被纠正回 `#home`。
- [completed] 3. 为 Nginx SPA 入口页增加 `no-store`，避免部署后继续使用旧的 asset manifest；保留 hash 静态资源命名。
- [completed] 4. 通过干净 Maven 测试、Docker Compose 重建、前端回归测试和当前容器浏览器验收。

结论：旧页面由“旧入口页缓存 + 管理员会话恢复时 `#home` 路由未被权限同步纠正”共同造成；当前容器已加载新前端包，管理员从 `#home` 会自动进入独立 `#admin` 工作台。

# 阶段二十五：趋势 AI 初审与人工终审轻量工作流（2026-09-15）

目标：把外部平台趋势内容从“抓取后直接展示”改为“程序过滤 → AI 初审 → 管理员终审 → 普通用户只读已通过内容”的可追踪流程，不引入 LangChain 或重量级工作流引擎。

## 第一性边界

- 定时抓取不依赖管理员会话；管理员负责来源刷新、待审处理和最终发布决定。
- AI 只能给出结构化审核建议，不能单独发布或驳回；AI 失败时内容必须停留在隔离状态。
- 普通用户趋势接口只返回人工终审通过且未下架的内容；下架仍可由管理员恢复。
- 审核记录不保存用户私有衣橱、认证凭据或完整外部页面正文，只保存必要的趋势元数据、审核结论和可追溯原因。

## 阶段

- [completed] 1. 增加趋势审核状态、AI 结论和人工终审记录的数据库结构。
- [completed] 2. 接入现有百炼客户端，增加结构化 AI 初审服务和失败重试边界。
- [completed] 3. 增加管理员待审列表、AI 建议、通过/驳回和审计接口。
- [completed] 4. 将普通趋势查询收敛到人工通过内容，并补齐前端审核工作台。
- [completed] 5. 完成反例测试、Docker 数据迁移、AI 关闭/失败和浏览器验收。

# 阶段二十六：运营总览 Lieflat Charts 数据可视化（2026-09-15）

目标：在不伪造历史趋势的前提下，使用本地 lieflat-charts 的 Lupi Basics SVG 模板，把当前运营聚合数据转成可数、可解释、可访问的图表。

- [completed] 1. 审计现有运营数据形状与 Lieflat Charts 候选模板，锁定图表契约。
- [completed] 2. 将账号构成、推荐运行和待处理队列接入本地 SVG 图表。
- [completed] 3. 补齐图表动效、无障碍说明、移动布局和数据空状态。
- [completed] 4. 完成构建、浏览器视觉验收与回归测试，并记录模板来源。
